/* WebClass configuration.
 * Default: local login/data, matching the original project.
 * Set a backend URL manually only when the PostgreSQL cloud bridge is ready.
 */
window.WEBCLASS_API_URL = window.WEBCLASS_API_URL || '';
